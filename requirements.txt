# Core dependencies
python-binance==1.0.19
ccxt==4.2.25
websockets==12.0
aiohttp==3.9.1

# DNS resolution dependencies
# Pin aiodns 3.0.0 to avoid pycares.ares_query_a_result AttributeError
# aiodns 3.1.1+ uses type hints that require pycares attributes not available in all versions
aiodns==3.0.0
pycares==4.4.0

# Data processing
pandas==2.1.4
numpy==1.26.2

# Async utilities
asyncio-throttle==1.0.2

# Configuration
python-dotenv==1.0.0

# Logging
colorlog==6.8.0

# Telegram Bot
python-telegram-bot==20.7

# Optional: Redis for caching (can be added later)
# redis==5.0.1
# aioredis==2.0.1
